export default function Footer() {
  return (
    <footer className="app-footer">
      Grid topology is a public, industry-standard synthetic test feeder (PNNL IEEE9500) — not real per-city
      utility infrastructure. Live heat, air quality, NOAA alerts, and EIA grid demand shown here are real,
      live API data.
    </footer>
  )
}
